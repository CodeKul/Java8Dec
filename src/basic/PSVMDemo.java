package basic;

import basic.p1.ClassOne;

public class PSVMDemo {

    /**access modifiers--
     * public,private,protected and default
     * private-allows methods or classes to be used only within the class
     * default-allows methods or classes to be used only within the package
     * protected-allows methods or classes to be used only within the package and outside the package
     * through inheritance
     *
     *
     * public--access modifier allows method to be invoked anywhere in the project
     * static-allows methods to be invoked without creating objects of the class
     * void--return nothing
     * main--entry point for java program execution
     */
    public static void main(String[] args) {
//        ClassOne obj=new ClassOne();
//        obj.showMessage();
    }
}
